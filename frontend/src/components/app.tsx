import * as React from "react";
import * as RB from "rebass";

import config from "config.json";

const App: React.FC = () => {
	// const [data, useData] = React.useState<String>(); 
	// const [data, useData] = React.useState<Record<string, string>>({}); 

	// Functions here

	return (
	<RB.Flex height="100%">
		<RB.Flex flexDirection="column">


		</RB.Flex>
	</RB.Flex>
	);
}


export {
	App,
};
